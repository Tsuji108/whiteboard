<% provide(:title, 'All users') %>
<h1>All users</h1>

<div class="center">
  <%= paginate @users %>
</div>

<ul class="index">
  <%= render @users %>
</ul>

<div class="center">
  <%= paginate @users %>
</div>
<% provide(:title, 'Past ml') %>
<h1></h1>

<div class="container">
  <div class="row">
    <div class="col-lg-8 col-lg-offset-2 col-md-8 col-md-offset-2 col-sm-10 col-sm-offset-1 col-xs-12">

      <p>
        <%= label :created_at, "送信日時" %>
        <div class="confirm-text"><%= (l @mailing_list.created_at, format: :long) %></div>
      </p>

      <%= render 'confirm_show_template' %>

      <%= link_to "戻る", :back, class: "btn btn-block btn-primary" %>
    </div>
  </div> <!-- row -->
</div><!-- container -->
